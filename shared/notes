Notes -

sudo yum install yum-downloadonly yumdownloader
cd; mkdir puppet-rpms
yum install puppet --downloadonly --downloaddir=puppet-rpms
Download puppet-server, puppet-dashboard, puppetdb-terminus to puppet-rpms
directory

-- Find out what is the equivalent command in Ubuntu

To Speed up installation, download the Chef Server to a local directory
There are two versions 11.1.3 and 11.1.4
chef_url=https://opscode-omnibus-packages.s3.amazonaws.com/ubuntu/12.04/x86_64/chef-server_11.1.4-1_amd64.deb
chef_url=https://opscode-omnibus-packages.s3.amazonaws.com/ubuntu/12.04/x86_64/chef-server_11.1.3-1_amd64.deb
#chef-server-core_12.0.0-rc.4-1_amd64.deb
pkg=$(mktemp);
wget "${chef_url}" -qO $pkg && sudo dpkg -i $pkg && rm $pkg


Install nginx
sudo su -c 'rpm -Uvh http://dl.fedoraproject.org/pub/epel/6/x86_64/epel-release-6-8.noarch.rpm'
yum install nginx


# sudo apt-get update >/dev/null 2>&1
# apt-get install -y
chef_url=https://opscode-omnibus-packages.s3.amazonaws.com/ubuntu/12.04/x86_64/chef-server_11.1.3-1_amd64.deb
# cd /home/vagrant
# wget --quiet $chef_url
